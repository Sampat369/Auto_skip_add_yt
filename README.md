# Auto_skip_add_yt from python
     This project is a real-time automation system that detects the appearance of advertisements during video playback on YouTube and triggers an early skip action without relying on the platform’s built-in skip button.  The system continuously monitors a specific region of the screen and uses Optical Character Recognition (OCR) -based detection .
